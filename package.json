{
  "name": "rive-animation-viewer",
  "version": "1.6.2",
  "description": "Simple local server to preview Rive animations",
  "main": "index.html",
  "scripts": {
    "start": "vite --host 0.0.0.0 --port 8080 --open",
    "serve": "vite --host 0.0.0.0 --port 8080",
    "dev": "vite",
    "build": "node scripts/build-dist.mjs",
    "tauri": "tauri",
    "build:full": "npm run build && npm run tauri build",
    "sync": "node scripts/sync-version.mjs"
  },
  "keywords": [
    "rive",
    "animation",
    "preview"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.0",
    "esbuild": "^0.27.0",
    "vite": "^7.2.2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "@codemirror/commands": "^6.10.0",
    "@codemirror/lang-javascript": "^6.2.4",
    "@codemirror/theme-one-dark": "^6.1.3",
    "@codemirror/view": "^6.38.8",
    "codemirror": "^6.0.2"
  }
}
